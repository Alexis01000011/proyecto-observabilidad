# Tomcat / JMX — evidencia de configuración

> Placeholder (M0). Se completa en M3/M8.

## Pendiente de documentar (M3)
- Versión del `jmx_prometheus_javaagent`.
- `CATALINA_OPTS` con `-javaagent:...=9404:/opt/jmx/config.yml`.
- App de ejemplo desplegada en Tomcat.
- Evidencia: `curl http://tomcat:9404/metrics`.
